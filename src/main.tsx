import React from 'react';
import ReactOM from 'react-dom/client';
import App from
·/app/App';
import './styles/index.css';
ReactOM. createRoot (document. getElementById(
'root')!). render (
<React. StrictMode>
<ApP>
</React. StrictMode>
）；
