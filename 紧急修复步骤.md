# 🔥 OpenAI API Key 泄露 - 紧急修复步骤

## ⚠️ 当前状态
- ✅ 已从代码中删除硬编码的 API Key
- ⏳ 需要推送修复到 GitHub
- ⏳ 需要生成新的 OpenAI API Key

---

## 🔧 立即执行的 3 个步骤

### 第 1 步：生成新的 OpenAI API Key

1. **访问 OpenAI API Keys 页面**
   ```
   https://platform.openai.com/api-keys
   ```

2. **创建新的 API Key**
   - 点击 "+ Create new secret key"
   - 名称：`AQUARION Production Key`
   - 点击 "Create secret key"

3. **复制并保存 API Key**
   - ⚠️ 只显示一次，务必保存好！
   - 格式：`sk-proj-...`

---

### 第 2 步：推送修复到 GitHub

代码已修复，但需要推送到 GitHub。

**如果你还有 GitHub Token：**
```bash
git push origin main
```

**如果 Token 已删除，生成新的 Token：**

1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 生成后复制 Token
5. 运行命令：
   ```bash
   git remote set-url origin https://chenr4673-sudo:你的新token@github.com/chenr4673-sudo/aquarion-website.git
   git push origin main
   ```

**或者直接在 GitHub 网页上操作：**

1. 访问 https://github.com/chenr4673-sudo/aquarion-website
2. 找到 `src/app/pages/AICoach.tsx` 文件
3. 点击编辑按钮（铅笔图标）
4. 找到第 118 行，改为：
   ```javascript
   const apiKey = import.meta.env.VITE_OPENAI_API_KEY;
   ```
5. 移除后面的硬编码 API Key
6. 提交更改

---

### 第 3 步：在 Vercel 配置新的 API Key

1. **进入 Vercel 项目**
   - 访问 https://vercel.com
   - 找到 `aquarion-website` 项目

2. **更新环境变量**
   - 点击 **Settings** → **Environment Variables**
   - 找到 `VITE_OPENAI_API_KEY`
   - 点击编辑，输入新的 API Key
   - 保存

3. **重新部署**
   - 回到 **Deployments** 页面
   - 点击最新的部署
   - 点击右上角 **...** → **Redeploy**

---

## ✅ 完成后检查

- [ ] GitHub 上的代码已无硬编码 API Key
- [ ] Vercel 环境变量已更新为新的 API Key
- [ ] 旧的 API Key 已被 OpenAI 吊销（已自动完成）
- [ ] 网站部署成功且 AI 教练可用

---

## 🛡️ 防止再次发生

### 永远不要这样做：
❌ 在代码中写 API Key
❌ 把 `.env` 文件提交到 Git
❌ 在公开的地方分享 API Key

### 正确的做法：
✅ 只在环境变量中配置 API Key
✅ 使用 `.env.example` 作为模板
✅ 在 Vercel/Netlify 等平台的设置中配置
✅ 定期轮换 API Key

---

## 💰 费用影响

- 旧的 API Key 已被吊销，不会产生新的费用
- 检查你的 OpenAI 账单，确认没有异常使用
- 设置使用限额：https://platform.openai.com/account/billing/limits

---

## 📞 需要帮助？

如果遇到问题，告诉我你在哪一步卡住了！
